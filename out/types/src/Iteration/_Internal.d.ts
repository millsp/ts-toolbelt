/** Describes an **`Iteration`**-**number** relationship
 */
export declare type FormatMap = {
    's': 2;
    'n': 3;
};
/** Describes the format of a **number**
 * - `s`: **`string`**
 * - `n`: **`number`**
 */
export declare type Format = keyof FormatMap;
